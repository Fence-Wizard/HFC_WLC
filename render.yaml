services:
  - type: web
    name: hfc-windload-wizard
    runtime: python
    buildCommand: pip install -e ".[dev]"
    startCommand: uvicorn app.application:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
